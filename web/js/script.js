function handleAction(actionName) {
    console.log(`Action triggered: ${actionName}`);
    
    // Optional: Call Python if needed
    // eel.log_action(actionName)
    
    // Visual feedback
    alert(`Action: ${actionName} simulated.`);
}
